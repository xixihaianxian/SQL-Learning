## 分页查询

- ### 1. 分页查询
  ```mysql
  select * from 表名 limit 查询索引,查询记录数
  ```
  
- ### 2. 注意

  - #### 起始索引从0开始，起始索引=（查询页码数-1）*每页显示记录数
  - #### 分页查询是数据库的方言，不同的数据库有不同的实现，mysql是limit
  - #### 如果查询的是第一页的数据，起始索引可以省略，直接写limit 10