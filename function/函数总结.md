## 函数总结

- ### 1. 字符串函数
  ```bash
  concat lower upper lpad rpad trim substring 
  ```

- ### 2. 数值函数
  ```bash
  ceil floor rand round mod
  ```

- ### 3. 日期函数
  ```bash
  curdate curtime now year month day date_add datediff
  ```

- ### 4. 流程函数
  ```bash
  if 
  ifnull 
  case when then else end
  case 字段 when val1 then else...end
  ```