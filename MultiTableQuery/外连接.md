## 外连接

- ### 1. 左外连接
  ```mysql
  select field from table1 left [outer] join table2 on condition;
  ```

- ### 2. 右外连接
  ```mysql
  select field from table right [outer] join table2 on condition;
  ```