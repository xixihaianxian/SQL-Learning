## 联合查询

- ### 1. 概念
  #### 对于union查询，就是把多次查询的结果合并起来，形成一个新的查询结果集。

- ### 2. 语法
  ```mysql
  select field form table1 union [all] select field from table2;
  ```
  
- ### 3. 注意事项
  - #### `union all`会将全部的数据直接合并在一起，`union`会对合并之后的数据去重。
